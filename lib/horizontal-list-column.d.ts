import { StyleProp, ViewStyle } from 'react-native';
interface Props {
    items: JSX.Element[];
    constainerStyle?: StyleProp<ViewStyle>;
}
export declare const HorizontalListColumn: ({ items, constainerStyle }: Props) => JSX.Element;
export {};
